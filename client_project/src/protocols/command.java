package protocols;


public final class command {
	public enum process{
		GET,
		KILL,
		START,
	}
	public enum application{
		GET,
		KILL,
		START,
	}
	public enum sreenshot{
	}
	public enum keylog{
	}
	public enum shutdown{
	}
}
